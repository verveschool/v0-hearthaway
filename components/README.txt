HearthAway component prototype

Files:
- components/CustomCursor.tsx
- components/ComponentsPage.tsx

CustomCursor.tsx is the global-ready cursor component.
ComponentsPage.tsx is the isolated playground page that mounts the cursor locally.

Import the page component from whichever route/folder your existing project uses for the /components page.
